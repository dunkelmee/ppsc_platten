ADMIN_PASSWORD=changeme
EVENT_NAME=Ping Pong Social Club
BASE_URL=http://localhost:8076

# Optional: set these to keep push subscriptions valid across server restarts.
# If unset, keys are auto-generated at startup (subscriptions reset on restart).
# VAPID_PRIVATE_KEY=
# VAPID_PUBLIC_KEY=
